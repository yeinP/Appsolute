<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.solution.appsolute.mapper.LoginMapper">
    <select id="selectByEmp_num" resultType="com.solution.appsolute.dto.login.LoginDto">
        SELECT * FROM employee WHERE emp_num = #{emp_num}
    </select>

</mapper>
