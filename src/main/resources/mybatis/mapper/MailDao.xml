<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.solution.appsolute.spring.dao.mail.MailDao">
    <select id="listDao" resultType="java.util.Map">
        select m.mail_num, m.mail_check, m.mail_sender, m.mail_receiver, e.emp_name, mail_title, mail_content, mail_Date
        from mail m inner join employee e on m.mail_receiver = e.emp_num where mail_sender = #{param1}
        union
        select m.mail_num, m.mail_check, m.mail_sender, m.mail_receiver, e.emp_name, mail_title, mail_content, mail_Date
        from mail m inner join employee e on m.mail_sender = e.emp_num where mail_receiver = #{param1} order by mail_num desc;
    </select>

    <select id="listAll" resultType="java.util.Map">
        select mail_num, mail_check, mail_sender, mail_receiver, mail_title, mail_content, mail_date from mail;
    </select>


    <select id="count" resultType="Integer">
        select count(*) from mail
    </select>


</mapper>